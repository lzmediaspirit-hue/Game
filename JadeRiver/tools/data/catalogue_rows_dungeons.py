"""V9f3 · the Part 8 room verticality catalogue rows V2d left partial (dungeons). Runs after catalogue.run(ROOMS) and
before the movement and verticality passes (tools/data/world.py build()). Fill run(); keep to this group's rooms."""


def run(rooms):
    pass
